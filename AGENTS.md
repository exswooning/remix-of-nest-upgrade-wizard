# AGENTS.md — Read this before you change anything

> **Mandatory for every AI agent (Claude Code, Cursor, Aider, Codex, etc.)**
>
> 1. Read this entire file before reading any source code or making any change.
> 2. Use it to orient: what the app is, which modules exist, where state lives, what conventions to honour.
> 3. After you ship a change, append an entry to the **Changelog** at the bottom (newest first). Keep it one line per change: `YYYY-MM-DD — short title — purpose (one clause)`. If you wrote multiple unrelated changes in one session, use one entry per change.
> 4. If a convention below is wrong or out of date, fix it in the same PR — don't just work around it.
> 5. Do not reorder existing changelog entries. Append new entries above the rest. Prune the bottom when the section grows past ~15 entries — older history lives in `git log`. AGENTS.md is loaded into every agent's context, so a bloated changelog is expensive on every turn.

---

## 1. What this project is

A single-page React + Vite + TypeScript app that bundles several internal tools used by the hosting business under one roof. The top-level UI is a tab strip; each tab is effectively its own mini-app.

**Top-level modules (the tabs):**

| Tab        | Acronym | Purpose                                                                                  | Entry file                                  |
| ---------- | ------- | ---------------------------------------------------------------------------------------- | ------------------------------------------- |
| UCAP       | Upgrade Cost / Add-on Pricing | Pro-rata upgrade calculator, pro-rata user add, billing ledger, VPS pricing, history | `src/pages/Index.tsx`                       |
| CGAP       | Contract Generation / Admin Panel | Letterhead-driven contracts, addendums, quick amendments, RfP, Service Order, SLA   | `src/pages/Index/CGAPEmbedded.tsx` → `src/pages/CGAP/*Tab.tsx` |
| QGAP       | Quotation Generation | Quote generation w/ TipTap editor and custom line items                                  | `src/pages/CGAP/QuotationTab.tsx`           |
| VRAP       | Vendor / Reseller quotes | Per-company quotation pipeline w/ layout anchors                                         | `src/pages/CGAP/VrapTab.tsx`                |
| TTAP       | AI assistant | Claude-API-driven assistant exposing app tools                                           | `src/pages/TTAPTab.tsx`                     |
| Database   | —       | Surfaces contracts, clients, activity log                                                | `src/pages/DatabasePage.tsx`                |
| Settings   | (admin) | Math settings, templates, project-default snapshot, user mgmt                            | `src/pages/CGAP/SettingsTab.tsx`            |

**Tech stack:** Vite 5, React 18, TypeScript, TailwindCSS, shadcn/ui (Radix primitives), react-router-dom, TanStack Query, TipTap (rich text), docxtemplater + pizzip (DOCX), pdf-lib + jspdf + html2canvas (PDF), mammoth + docx-preview (DOCX preview), Supabase JS (auth + some persistence via Lovable Cloud), Sonner (toasts).

**Where state lives:**

- **`localStorage`** is the primary persistence layer. Almost everything — letterheads, template assignments, RfP/VRAP anchor layouts, SLA section text, QGAP/CGAP settings, calculation history, activity log — is stored there per-browser.
- **`src/data/defaults.json`** is bundled with the build and seeded into `localStorage` on first load (see [src/utils/seedDefaults.ts](src/utils/seedDefaults.ts) and `seedDefaults()` called from [src/main.tsx](src/main.tsx)). Seeding only fills *missing* keys — existing user values are never overwritten unless the user clicks "Reset to project defaults" in Settings.
- **Supabase** (via `@supabase/supabase-js`, configured in [vite.config.ts](vite.config.ts) with `LOVABLE_CLOUD_URL`) backs auth + a handful of shared tables (contracts, users, RfP archive). Most app state is still localStorage.
- **`useAuth()`** ([src/contexts/AuthContext](src/contexts/)) exposes `currentUser`, `currentUsername`, `isAdmin`, `logout`, `getPlanData()`. Plan data drives the UCAP calculator.

**How to run:** copy `.env.example` to `.env.local` and fill the Supabase keys, then `npm run dev` → http://localhost:8080. Build: `npm run build`. Lint: `npm run lint`. Smoke test (Node-based browser check): `npm run smoke:browser`. Without env vars the app throws at boot — see [src/integrations/supabase/client.ts](src/integrations/supabase/client.ts).

**Deployment:** see §6 below. The Supabase project is self-owned (not Lovable Cloud); the frontend deploys to Vercel/Netlify/Cloudflare Pages as a static SPA.

---

## 2. Conventions — honour these

### 2.1 Activity log

Every user-visible action that produces a side effect (PDF generated, calculation run, template uploaded, setting saved, login/logout) **must** call `logActivity({ kind, module, action, meta })` from [src/utils/activityLog.ts](src/utils/activityLog.ts). The Database tab surfaces this. If you add a new feature that ships output, log it.

```ts
import { logActivity } from "@/utils/activityLog";

logActivity({
  kind: "pdf",                  // 'pdf' | 'calculation' | 'action' | 'auth'
  module: "CGAP/Contract",      // 'UCAP/Upgrade' | 'CGAP/Contract' | 'QGAP' | 'VRAP' | 'Settings' | …
  action: "Contract PDF generated",
  meta: { client, total },      // free-form; surfaces in the Database tab
});
```

The log is capped at 500 entries (FIFO) and lives in `localStorage` under `"activity-log"`. It is **per-browser**, not shared across users.

### 2.2 localStorage keys & defaults seeding

- Pick a clear, prefixed key (e.g. `vrap-companies`, `cgap-settings`, `qgap-quotes`, `rfp-layout-v2`). Don't use generic keys like `"settings"`.
- If the key represents shippable project state (templates, layouts, defaults), it gets snapshotted into `src/data/defaults.json` via the "Export project defaults" button in Settings.
- If the key is per-user / per-session (auth, current username, history), **add it to `NON_SHIPPABLE_KEYS`** in [src/utils/seedDefaults.ts](src/utils/seedDefaults.ts). Otherwise it will leak into the bundled defaults and every fresh visitor will inherit your local state.
- Wrap all `localStorage` reads in `try/catch` and tolerate missing/corrupt JSON — defaults seeding does this; copy the pattern.

### 2.3 Document anchor layouts (RfP, VRAP, letterheads)

RfP and VRAP tabs render documents over a fixed background image (letterhead). Field positions are stored as anchor coordinates in `localStorage` (`rfp-layout-*`, `vrap-layout-*`). When you touch these tabs:

- Don't move anchor logic out of `src/utils/rfpAnchors.ts` / `src/utils/vrapLayout.ts` — the designer mode and the PDF exporter both consume from there.
- PDF output uses **vector text via pdf-lib** (added in `e08c00c`), not html2canvas rasterisation, so anchors must be coordinate-based.
- Inspector opens immediately in designer mode (changed in `1210f4c`); keep that behaviour.

### 2.4 UCAP math

Order of operations in [src/pages/Index.tsx](src/pages/Index.tsx) (`calculateUpgrade`):

1. Resolve plan price (override > pricing[cycle] > flat price).
2. Apply **discount** (`price *= 1 - discount/100`).
3. Apply **tax exclusion** (`price *= 1 - tax/100`) — yes, this subtracts tax rather than adding it; that's the intentional behaviour for "tax-exclusive" pricing.
4. Hand off to `calculateUpgradeWithSettings()` in [src/utils/calculationEngine.ts](src/utils/calculationEngine.ts) which respects admin math settings (rounding, etc.).

Don't reorder these without checking the Settings tab's `MathSettings` first.

### 2.5 UI

- Tabs use the `glass-tabs` / `glass-tab` classes defined in [src/index.css](src/index.css). Keep new tab strips consistent.
- Dark mode is a top-level boolean threaded as `darkMode` prop. There is no theme context — pass the prop.
- Toasts: use `useToast()` from `@/hooks/use-toast` (shadcn) for inline feedback. `sonner` is also installed; prefer the shadcn one for consistency.
- shadcn primitives live in `src/components/ui/`. Don't add new Radix wrappers — extend the existing ones.

### 2.6 TipTap & docx pipelines

- TipTap is used for rich text editing inside CGAP (`RichDocumentEditor`). Don't swap it for another editor without a discussion — `4d76285` migrated the whole CGAP stack to it.
- DOCX rendering uses `docxtemplater` + `pizzip` for templated merges and `mammoth` / `docx-preview` for in-browser preview.
- PDF generation uses `pdf-lib` (vector path, preferred) or `jspdf` + `html2canvas` (rasterised, legacy). New work should use pdf-lib.

### 2.7 What NOT to commit

- Anything under `NON_SHIPPABLE_KEYS` in defaults.json (auth tokens, usernames, calculation history).
- Real customer data in `defaults.json` — strip PII before committing a snapshot.
- `.env` files or secrets. The Supabase publishable key in `vite.config.ts` is the public anon key — that's fine; the service key is not in this repo.

---

## 3. Gotchas

- **Vite dev port is `8080`** (not 5173). Configured in `vite.config.ts`.
- **Lovable tagger** runs in dev mode only (`mode === 'development'` in plugins). If something looks weird with component tagging in prod, that's why.
- **First load with empty localStorage** runs `seedDefaults()` from [src/main.tsx](src/main.tsx). If you add a feature that depends on a localStorage key, also ship a default in `src/data/defaults.json` — otherwise the feature looks broken on fresh installs.
- **Plan data lives in `useAuth().getPlanData()`** — it's not a static import. New UCAP categories/plans need an Auth context update, not just a constant.
- **`calculator-username` vs `calculator-user`** — `currentUser` returns the user's display name, `currentUsername` is the login slug. The activity log prefers `calculator-username` and falls back to `calculator-user`.
- **CGAP route is embedded inside the Index tab**, not a separate route (`src/pages/Index/CGAPEmbedded.tsx`). There's also a `src/pages/CGAP/CGAPApp.tsx` standalone — both exist; don't delete one assuming it's dead.
- **shared-hosting category** filters out plans whose name contains "Cloud" from target plan dropdowns (legacy product rule).
- **Smoke test** (`scripts/browser-smoke-check.mjs`) is a Node-based puppeteer-ish check that boots the app and looks for crashes. Run it before any structural change.

---

## 4. Working agreements for agents

- **Read the file you're about to edit, in full, before editing.** Even small refactors break invariants you didn't know existed.
- **Before deleting a file or symbol**, grep for usages. There are two CGAP entry paths (embedded and standalone) and several near-duplicate tabs (Contract / Addendum / QuickAmendment share a layout pattern but are not identical).
- **Don't introduce new persistence layers.** localStorage + the existing Supabase tables are the sanctioned stores. If you genuinely need a new table, write the migration script and put it under `scripts/`.
- **Match the existing commit-message style** — recent commits are short imperative summaries (`"Add ..."`, `"Rebuild ..."`, `"QGAP: ..."`). Avoid the bare `"Changes"` placeholder used in earlier history.
- **Run `npm run dev` and click through the affected tab** before declaring a UI change complete. Type-checking is not enough — these tabs render documents pixel-by-pixel and regressions are visual.
- **Append to the Changelog below.** Every change. No exceptions.

---

## 5. Deployment

This app no longer depends on Lovable Cloud. The Supabase project is one you own directly at supabase.com; the frontend deploys as a static SPA to Vercel/Netlify/Cloudflare Pages.

### 5.1 Environment variables

| Var                              | Where it lives                                                                                  | Notes                                                                                                                              |
| -------------------------------- | ----------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `VITE_SUPABASE_URL`              | `.env.local` (dev), hosting provider env vars (prod)                                            | Project URL from Supabase → Settings → API.                                                                                        |
| `VITE_SUPABASE_PUBLISHABLE_KEY`  | same                                                                                            | "anon public" key from Supabase → Settings → API. Safe to ship in the browser bundle.                                              |
| `DATABASE_URL`                   | Hosting provider env vars only (build step). Optional for local builds — `migrate.mjs` no-ops without it. | Postgres URI from Supabase → Settings → Database → "Session pooler" connection string. Used by `scripts/migrate.mjs` during the build to apply `supabase/migrations/*.sql`. |
| `SUPABASE_SERVICE_ROLE_KEY`      | **Edge Function env only** — never `VITE_*`, never in the repo, never in the browser              | Service-role key. Stored in Supabase → Edge Functions → Secrets when we add Edge Functions for upload/download.                    |

**Never** put the service-role key behind a `VITE_` prefix — Vite inlines those into the JS bundle, which would publish your admin credentials.

### 5.2 First-time setup (new Supabase project)

1. Create the project at supabase.com (free tier; Singapore or Mumbai region for Nepal latency).
2. Copy `.env.example` to `.env.local` and fill in `VITE_SUPABASE_URL` + `VITE_SUPABASE_PUBLISHABLE_KEY`.
3. Run schema migrations: `DATABASE_URL=<postgres-uri> npm run migrate`. Migrations live in `supabase/migrations/*.sql` and apply in filename order; the runner tracks them in a `schema_migrations` table so re-runs are idempotent.
4. `npm run dev` — confirm the app boots and Supabase calls succeed.

### 5.3 Vercel deploy

1. Push to GitHub (already done — `github.com/exswooning/remix-of-nest-upgrade-wizard`).
2. https://vercel.com/new → import the repo. Framework preset: **Vite**. Build command: `npm run vercel-build` (this runs `migrate.mjs` then `vite build`). Output dir: `dist`.
3. Project Settings → Environment Variables:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_PUBLISHABLE_KEY`
   - `DATABASE_URL` (the Session-pooler URI, not the Direct one — pooler is what Vercel's build environment can reach without IPv6)
4. Deploy. First build runs migrations against the new Supabase, then builds the SPA.

Netlify and Cloudflare Pages work the same way; only the env var UI and build-command field locations differ.

### 5.4 If you ever need to rotate keys

- `VITE_SUPABASE_PUBLISHABLE_KEY` leak: rotate via Supabase → Settings → API → "Generate new anon key". Update `.env.local` + Vercel env. Redeploy.
- `SUPABASE_SERVICE_ROLE_KEY` leak: same place, "Generate new service-role key". Update Edge Function secrets. **Audit Storage and tables for unexpected writes between leak and rotation.**

---

## 6. Changelog

> Format: `YYYY-MM-DD — short title — purpose (one clause). [commit-sha]`
>
> Newest first. Append above the existing entries; do not edit or remove past entries.

- 2026-06-02 — **DCAP inline editor: PyMuPDF Python sidecar for sejda-quality in-place text editing** — user complained the local mask-and-draw left visible white-out scars in tinted bands when deleting text, vs sejda.com's truly invisible deletion. Investigation: (a) Stirling-PDF doesn't have in-place text editing (only annotations / stamps — verified via docs.stirlingpdf.com/functionality); (b) sejda's OSS Java library (which our `sejda-backend/` wraps) has no `EditTextTask` — that's commercial-only; (c) pdf-lib content-stream surgery is brittle; (d) **PyMuPDF** has `page.add_redact_annot(rect, text=...)` + `apply_redactions()` that actually rewrites the page's `Tj`/`TJ` operators — same approach sejda's commercial product uses. Built the sidecar. (1) New [pymupdf-backend/](pymupdf-backend/) — Python 3.12 + FastAPI 0.115 + PyMuPDF 1.24.14 + python-multipart. [app.py](pymupdf-backend/app.py) (~220 lines) exposes `GET /api/health` + `POST /api/edit-text` (multipart `file` PDF + `edits` JSON array of `TextModification`). Edits grouped per page; one `apply_redactions()` per page batch; coordinate flip from PDF bottom-left to PyMuPDF top-left; standard PDF 14 fonts via `_pick_pymupdf_font(family, bold, italic)` matching `helv`/`tiro`/`cour` × 4 weight-style variants; `cross_out=False` to suppress PyMuPDF's default redaction cross-out; descender slack at 0.18 × fontSize to cover g/p/y/q without bleeding into the line above. Output via `doc.write(garbage=4, deflate=True, clean=True)`. (2) [pymupdf-backend/Dockerfile](pymupdf-backend/Dockerfile) is single-stage `python:3.12-slim` (PyMuPDF ships pre-built wheels — no apt-get for mupdf-dev needed); non-root `pymupdf:1001` user. [docker-compose.yml](pymupdf-backend/docker-compose.yml) binds 127.0.0.1:8090, urllib-based healthcheck, 30 MB JSON log ceiling. (3) Frontend client: [src/utils/pymupdfBackend.ts](src/utils/pymupdfBackend.ts) — `isPymupdfBackendConfigured()` / `pingPymupdfBackend(timeoutMs)` / `applyTextModificationsViaBackend(bytes, mods)`. (4) [src/components/PdfEditorContainer.tsx](src/components/PdfEditorContainer.tsx) pings the backend on mount and shows a teal **PyMuPDF** badge in the toolbar when healthy, amber **Backend offline** badge when `VITE_PYMUPDF_URL` is set but unreachable; Save prefers the backend, transparently falls back to local `applyTextModifications` on any backend error so the editor never breaks. Mode is logged to the activity log meta. (5) [.env.example](.env.example) documents the new `VITE_PYMUPDF_URL` slot. (6) AGPL licence — fine for internal use; documented redistribution caveat in [pymupdf-backend/README.md](pymupdf-backend/README.md). **Untested at write time** — needs `docker compose up` on a box with Docker to verify, but the FastAPI + PyMuPDF pattern is well-trodden and matches PyMuPDF's official redaction-with-replacement example. [pending]
- 2026-06-02 — **DCAP: canvas-overlay inline PDF text editor — three-tier sandwich (canvas + DOM + pdf-lib mask-and-draw)** — built a sejda-style click-to-edit text editor as a first-class sub-view of the DCAP tab, separately from the existing tool-grid / Stirling iframe. (1) DCAP tab now opens to a button-row chooser between **Inline editor** (new) and **Tools** (existing Stirling-or-pdf-lib path); inline editor is default so users see the new feature first. (2) [src/components/PdfEditorContainer.tsx](src/components/PdfEditorContainer.tsx) is the orchestrator — loads a PDF via pdfjs-dist (`?worker` import reused from `pdfTools.ts`, now-exported `ensurePdfjsWorker()`), renders each page to a `<canvas>` at `viewport.scale * devicePixelRatio` backing-store + CSS-sized at `viewport.scale` for razor-sharp Retina output, walks `page.getTextContent()` and emits one `TextItemDescriptor` per non-empty run with the screen-px placement + PDF-pt placement + sampled page-bg colour (via `getImageData(1px ~30% above the glyph top)`) + bold/italic detection from the resolved font name. (3) [src/components/EditableTextNode.tsx](src/components/EditableTextNode.tsx) is the per-glyph-run node — fully transparent by default so the underlying canvas glyphs show through, hovered with a 1px dashed teal outline (CSS rule already in [src/index.css](src/index.css#L243)), and on click flips to `contentEditable` painted with the sampled page bg so the user is "editing in place" over the canvas. Three-stage smart fit on every keystroke: stage 1 measure with original size, stage 2 tighten letter-spacing down to -0.08em, stage 3 proportionally shrink font-size (floor 4 px) — `ctx.measureText` based, so the box never bumps neighbours regardless of input length. (4) [src/utils/pdfLibExporter.ts](src/utils/pdfLibExporter.ts) is the save engine — `applyTextModifications(originalBytes, mods)` groups mods by page, stamps a vector rectangle in the sampled bg colour (1.5 pt pad + 0.22 × font descent) over each original glyph box, then draws the new text on top in the matched standard PDF font (Helvetica / Times-Roman / Courier × {regular, bold, italic, boldItalic}) at the adjusted size from stage 3. Output is a fresh `Uint8Array`. (5) Activity log: `kind: 'pdf'`, `module: 'DCAP/InlineEditor'`. (6) Stable item ids `p{page}-i{itemIdx}` so the modifications Map survives re-renders and the export walks in deterministic order. (7) Caveats — standard PDF fonts only (the exact original TTF isn't subsettable from `pdfjs-dist` to `pdf-lib` without a backend); letter-spacing currently ignored on export (only font-size adjustment is honoured) — same fidelity as Acrobat's "edit text" tool. Three deliverable files match the spec verbatim. [pending]
- 2026-06-02 — **CGAP/ContractTab: split into 4 sibling modules to cut per-edit token cost** — `src/pages/CGAP/ContractTab.tsx` was the heaviest source file in the project at 108 KB / 2,157 lines, getting re-uploaded every turn the user edits the contract tab. Extracted four cleanly-separable concerns into their own files: (1) [src/utils/letterheadToDataUrl.ts](src/utils/letterheadToDataUrl.ts) — the canvas-based image fetcher (33 lines) — also makes the helper reusable from any other PDF generator. (2) [src/utils/contractToggles.ts](src/utils/contractToggles.ts) — `ContractToggles` interface + `loadUserDefaultToggles` / `saveUserDefaultToggles` against `contract-user-default-toggles` localStorage key (36 lines). (3) [src/pages/CGAP/contractDefaults.ts](src/pages/CGAP/contractDefaults.ts) — `TEST_DATA` fixture + `DEFAULT_NEW_FIELDS` (bank slot seeding) + `AUTO_FIELDS` (the form-locked-fields allow-list — paymentWords / contractPeriod / companyAbv) (45 lines). (4) [src/components/SectionBodyEditor.tsx](src/components/SectionBodyEditor.tsx) — the per-row wrapper around `SectionEditor` with the format-risk badge, mini-preview toggle, edit-as-text toggle, and fix-formatting button (133 lines). ContractTab dropped from 2,157 → 1,958 lines / 108 KB → 100 KB. Also tidied lucide-react + `@/components/ui/textarea` + `SectionEditor` + `sectionHtmlCleaner` imports that were only used by the extracted block. All four new files transform cleanly via Vite at `http://localhost:8080/<path>`. [pending]
- 2026-06-01 — **DCAP: Stirling PDF embedded as the "Sejda clone"** — instead of continuing to reimplement sejda.com's polished editor from scratch (which would never match without a commercial SDK), wired up [Stirling PDF](https://github.com/Stirling-Tools/Stirling-PDF) — the open-source self-hosted PDF toolkit with merge / split / rotate / OCR / watermark / sign / inline-edit text / forms / redact / convert / compress, all in a polished web UI. (1) New [stirling-pdf/](stirling-pdf/) sub-project with `docker-compose.yml` that runs `docker.stirlingpdf.com/stirlingtools/stirling-pdf:latest` on port 8084 with bind-mounted volumes for OCR training data, configs, custom files, and logs; UI branding tweaked via `UI_APPNAME` / `UI_APPNAMENAVBAR` env vars so it reads "DCAP — PDF Tools" inside the iframe. Healthcheck + 30 MB JSON log ceiling identical to the sejda-backend pattern. (2) [src/pages/DCAPTab.tsx](src/pages/DCAPTab.tsx) rewrites to check `VITE_STIRLING_URL`: when set, renders an iframe of the Stirling UI sized to `calc(100vh - 220px)` with a header strip showing the URL + an "Open in new tab" button; iframe sandbox is permissive (`allow-same-origin allow-scripts allow-forms allow-downloads allow-popups allow-modals`) because Stirling needs all of that for file uploads + workers + downloads. When the env var is empty, falls back to the existing `SejdaToolGrid` (pdf-lib tile grid) so the tab still works without the Docker container. (3) [stirling-pdf/README.md](stirling-pdf/README.md) covers setup, local run, production deploy, and the storage layout. (4) [.env.example](.env.example) documents the new `VITE_STIRLING_URL` slot alongside the existing `VITE_SEJDA_BACKEND_URL`. (5) Stirling and sejda-backend are independent — sejda-backend exposes 4 REST endpoints called from the legacy pdf-lib panel; Stirling is a full UI iframed into the tab. Run either, both, or neither. [pending]
- 2026-06-01 — **`poq-maker/` — standalone Next.js 15 quote/invoice generator** — user spec'd a separate production-ready Next.js 15 + React 19 + Tailwind v4 app (their own `package.json`, Dockerfile, docker-compose) at the repo root in `poq-maker/`. The original ask was ambiguous about whether this should be a tab in the existing Vite SPA or a separate app — I called out the contradiction in the response and built the standalone version per the explicit tech-stack spec. Files: [poq-maker/package.json](poq-maker/package.json) (Next 15.3.8, React 19, lucide-react 0.525.0, html2canvas + jspdf + jspdf-autotable, scripts: `next dev --turbopack` / build / start / lint), [next.config.ts](poq-maker/next.config.ts) with `output: 'standalone'` for the tiny Docker image, [postcss.config.mjs](poq-maker/postcss.config.mjs) using `@tailwindcss/postcss` (Tailwind v4 has no separate config — theme tokens in `globals.css` under `@theme {}`), App-Router shell ([layout.tsx](poq-maker/src/app/layout.tsx) + [page.tsx](poq-maker/src/app/page.tsx) + [globals.css](poq-maker/src/app/globals.css)), [QuoteForm.tsx](poq-maker/src/components/QuoteForm.tsx) (company/client/meta sections + dynamic line-items table with per-row tax % and live totals readout), [QuotePreview.tsx](poq-maker/src/components/QuotePreview.tsx) (A4-shaped 794×1123 px capture surface, minimalist serif quote layout, forwardRef so the page can lift the DOM node for capture), [exportPdf.ts](poq-maker/src/utils/exportPdf.ts) (lazy-imports html2canvas + jspdf at click-time; renders at `scale: 2`; slices the source canvas into A4 pages when content overflows, addImage per page with a clean per-page offset). [Dockerfile](poq-maker/Dockerfile) is 3-stage `node:22-alpine` (deps→builder→runner), runs as non-root `nextjs:1001`, ships `wget` for the compose healthcheck. [docker-compose.yml](poq-maker/docker-compose.yml) binds strictly to `127.0.0.1:3000`, loads `.env`, sets NODE_ENV=production, 30s wget healthcheck with start-period, isolated `poq-net` bridge, JSON log ceiling at 10 MB × 3 files = 30 MB. **Untested at write time** — no Node 22 / Next 15 install on this dev box, but the file structure + Tailwind v4 setup matches Next's official `create-next-app@latest` output. [pending]
- 2026-06-01 — **DCAP editor: font matching + pixel-sampled background + canvas-measured text width** — user pushed back that the click-to-edit text boxes (a) used the wrong font (always Helvetica), (b) covered the original text with pure white when the page bg was tinted, (c) were too wide because the width was a loose heuristic. Three fixes. (1) **Font detection**: `extractPageTextItems` now reads pdfjs's `commonObjs` to resolve the opaque "g_d0_f1" id to the real font name (e.g. "TimesNewRoman", "Helvetica-Bold"). New `guessWebFontFamily(pdfFontName)` maps that to a web font family — Courier-like → mono, Helvetica/Arial → sans, Times/Roman/serif → serif, default → serif (most of our contracts are Times). Bold + italic also detected from the name. Surfaced via new fields on `PdfTextItem` (`webFontFamily`, `bold`, `italic`). (2) **Background colour sampling**: in [src/components/PdfPageEditor.tsx](src/components/PdfPageEditor.tsx), once the page image loads we draw it to an off-screen canvas and call `ctx.getImageData(x, y, 1, 1)` for each text run — sampling ~15 % of the text height ABOVE the glyph top so we hit page background, not the text itself. Stored as `bgColor` (CSS rgb string) for the editor + `bgRgb` (0..1 floats) for the pdf-lib stamp. The editable span now uses that as its `background` when edited, AND the white-out rect emitted on Apply uses it as the rect fill — so editing inside a tinted table cell doesn't leave a white scar. (3) **Canvas `measureText`**: width is now measured via `ctx.measureText(text).width` with the resolved `font` shorthand (`italic bold ${fontSizePx}px ${family}`) so the editable box hugs the actual glyphs. The text span uses `display: inline-block` once edited so it auto-grows as the user types. (4) **White-out padding**: stamp now extends 2 pt either side of the original glyph box to swallow anti-aliased edges of the underlying text. (5) Caveat — the SAVED PDF still stamps the replacement text in Helvetica via pdf-lib's `StandardFonts` because we don't embed a Times TTF; the screen render looks right, the download approximates. Full font fidelity in the saved file needs an embedded font follow-up. [pending]
- 2026-06-01 — **DCAP editor: sejda-style click-to-edit on existing PDF text** — user side-by-sided DCAP against sejda.com and called out the missing inline-text-editing flow. Built it via pdfjs's text layer. (1) New `extractPageTextItems(doc, pageIndex)` in [src/utils/pdfTools.ts](src/utils/pdfTools.ts) walks `page.getTextContent()` and emits `PdfTextItem[]` with each run's text + font size + position in PDF user-space points. (2) [src/components/PdfPageEditor.tsx](src/components/PdfPageEditor.tsx) loads text items in parallel with the page image; each item becomes a `position: absolute` `<div>` overlay sized to the original glyph box (x = `xPt * RENDER_SCALE`, y flipped against page height, font size scaled). (3) Initial state: each text span is invisible (`background: transparent` + `WebkitTextFillColor: transparent`) so the rendered page image's pixels show through cleanly. Hover styles via new `.opc-text-span:hover` rule in [src/index.css](src/index.css) (1px dashed teal outline) signal the text is clickable. (4) Click in select mode → span flips to `contentEditable`, background → `#fff` (white-out), `WebkitTextFillColor` → user-picked colour → the new text paints over the original glyphs. (5) Floating mini-toolbar appears above the actively-edited span: **B / I / size −/+ (with live px readout) / colour picker / revert** — matches sejda's per-element context bar. (6) On **Apply changes** (renamed from "Done"), each edited span becomes a pair of overlays: a `rect` (white, alpha 1) sized to the original glyph box + a `text` overlay at the baseline with the edited content. pdf-lib stamps both on next Save. (7) Toolbar hint copy updated: "Click any existing text to edit it in place. Drag overlays to move." (8) Caveats — font matching is approximate (we use Helvetica for the replacement; original font face isn't preserved); the `width` heuristic falls back to `fontSizePx * char-count * 0.5` when pdfjs reports zero. [pending]
- 2026-06-01 — **CGAP → One-page Contract tab** — new condensed contract format that fits on a single A4 page, for short engagements where the 9-page full Contract is overkill. New file [src/pages/CGAP/OnePageContractTab.tsx](src/pages/CGAP/OnePageContractTab.tsx) ships a focused form (client name / address / coordinator / titles, product, term, users, effective date, amount + auto-words, payment schedule, bank picker from `loadBankSlots`, provider signatory) + a live single-page A4 preview with five condensed clauses (Services / Payment / Term & Termination / Confidentiality / Governing Law) in the same 42 mm | 1fr two-column grid the full Contract uses; signature row at the bottom in a 50/50 grid for client + provider names + titles. Download path uses html2canvas at scale 2 to capture the preview surface, jsPDF assembles a 1-page A4 PDF; auto-archives to the Supabase `contracts` bucket under `${currentUsername}/${contract_id}.pdf` if the user is signed in; activity log entry written under `module: CGAP/OnePageContract`. Contract id auto-computed from `companyAbv` + effective date in the same `YD-NNBS-YY-MM-DD-V` shape; abbreviation auto-derived from client name on edit. Bank dropdown sources slots from `loadBankSlots()` and prints `Bank · A/C · No.` inline in the payment clause when one is picked. Registered in [src/pages/Index/CGAPEmbedded.tsx](src/pages/Index/CGAPEmbedded.tsx) as the second CGAP sub-tab (between Contract and Addendum) with a `FileMinus` icon; grid bumped from 8 → 9 cols. [pending]
- 2026-06-01 — **DCAP UI: sejda.com-style tile grid + focused per-tool views** — user wanted the DCAP tab to match the sejda.com UX, not just one big toolbar. New [src/components/SejdaToolGrid.tsx](src/components/SejdaToolGrid.tsx) is the whole thing in one file: (1) **Landing**: glass-card header + a responsive grid (2 / 3 / 4 cols at sm / md / lg) of teal-iconed tile cards — Merge / Split / Rotate / Extract / Compress / PDF→Images / Images→PDF / Page numbers / Watermark / Header-Footer / OCR / Edit. Each card has icon + title + one-line blurb, hover-lifts to teal border. (2) **Per-tool view** (tile clicked): back-button header with the tile's title/blurb; full-width dashed drop zone (acceptsensitive to tool — PDFs / images / both); files-list card with name + size + remove-X; tool-options card with the right form fields for each op (page-range string for extract, quality slider for compress, format radio + scale for pdf→images, format + 6-position picker for page numbers, range slider + font for watermark, header/footer inputs, language code for OCR); progress strip + Clear / Apply buttons at the bottom. (3) Apply handler converts uploads to `LoadedPdf[]` (running images through `imageFileToLoadedPdf` so they merge cleanly), runs the right utility from `src/utils/pdfTools.ts`, downloads the result via Blob. (4) The "Edit" tile delegates to the existing `PdfToolsPanel` because its per-page editor flow doesn't fit the one-shot tile pattern. (5) [src/pages/DCAPTab.tsx](src/pages/DCAPTab.tsx) collapses to a one-line wrapper around the new grid. Per-page edit flow inside Contract tab's PDF Tools panel still uses the legacy `PdfToolsPanel` — left alone to avoid regressions there. Sejda backend toggle on the legacy PdfToolsPanel still works; wiring it into the new tile views is a follow-up. [pending]
- 2026-06-01 — **sejda-backend: installed Java 21 + Maven, compiled, fixed Sejda 5.x API errors, verified end-to-end** — `brew install openjdk@21 maven` on the dev box (openjdk@21 is keg-only; JDK at `/opt/homebrew/opt/openjdk@21/bin/java`, Maven 3.9.16 picks up its own bundled openjdk-26 to run). First `mvn package` surfaced four Sejda 5.x API mismatches I'd guessed wrong: (1) `SingleTaskOutput` is an interface (not a functional interface), can't use a lambda; (2) `RotateParameters` / `ExtractPagesParameters` / `SplitByEveryXPagesParameters` `setOutput()` expects `SingleOrMultipleTaskOutput` — fix is to use `FileOrDirectoryTaskOutput.file(file)` / `.directory(dir)` instead of `FileTaskOutput` / `DirectoryTaskOutput`; (3) `ExtractPagesTask` is not generic — drop the `<…>`; (4) the split task `SplitByEveryXPagesTask` doesn't exist — the params class is `SplitByEveryXPagesParameters` and gets dispatched by Sejda's `TaskExecutionService`. (5) Even after the API fixes, calling `new MergeTask().execute(params)` directly throws NPE because `outputWriter` is set up in `init()` which the direct-execute path skips — Sejda tasks need `new DefaultTaskExecutionService().execute(params)` instead. Refactored [PdfController.java](sejda-backend/src/main/java/com/nestnepal/sejda/PdfController.java) to a single `private final TaskExecutionService sejda = new DefaultTaskExecutionService();` field used by all four endpoints. (6) Verified end-to-end via curl: `/api/merge` (2 single-page PDFs → 2-page PDF, confirmed via pdf-lib `getPageCount()`), `/api/rotate` (90°), `/api/split` (2 chunks zipped). `/api/health` returns `ok`. Service comes up in <1 s, listens on port 8089. (7) `setOutputPrefix` token list — Sejda recognises `[CURRENTPAGE]` and `[FILENUMBER]`, NOT `[TOTALFILESNUMBER]` (gets emitted literal in filenames); switched to `chunk-[FILENUMBER]-page-[CURRENTPAGE]-`. Build artifact lives in `sejda-backend/target/sejda-backend-0.1.0.jar`; run with `java -jar`. [pending]
- 2026-05-31 — **sejda-backend: optional Java/Spring Boot service wrapping the real Sejda library** — user wanted to actually use the code from `torakiki/sejda` (Java) instead of our JS reimplementation. Built it as a separate sub-project so the frontend's Vercel deploy is unaffected. (1) New [sejda-backend/](sejda-backend/) directory — Spring Boot 3.3 + Java 21 + `org.sejda:sejda-sambox:5.1.16`. (2) `pom.xml` is intentionally minimal — just Spring Boot starter web + sejda-sambox. (3) [SejdaApplication.java](sejda-backend/src/main/java/com/nestnepal/sejda/SejdaApplication.java) wires CORS (default `*`, override via `SEJDA_ALLOWED_ORIGIN` env). (4) [PdfController.java](sejda-backend/src/main/java/com/nestnepal/sejda/PdfController.java) exposes `GET /api/health` + 4 ops: `POST /api/merge` (multipart `files`, ≥2 PDFs), `POST /api/rotate` (`file` + `degrees` 90/180/270), `POST /api/extract` (`file` + `pages` like `"1,3,5-7"`), `POST /api/split` (`file` + `pagesPerChunk` → ZIP). Each follows Sejda's `Parameters → task.execute(params)` model with a `ByteArrayOutputStream` → bytes pipeline. (5) `application.properties` sets port `$PORT` (Render/Railway/Fly inject this; defaults to 8089), 200 MB multipart limit, 5-min async timeout for long ops. (6) Two-stage Dockerfile (maven build → JRE runtime). (7) [sejda-backend/README.md](sejda-backend/README.md) covers run-locally + the three deploy targets. **Important caveat**: the Java is unverified — no JVM in this dev env. User needs `mvn package` on a Java 21 box to confirm compile before deploying. (8) Frontend integration: new [src/utils/pdfBackend.ts](src/utils/pdfBackend.ts) thin client (`backendMerge` / `backendRotate` / `backendExtract` / `backendSplit` / `pingBackend`). (9) [src/components/PdfToolsPanel.tsx](src/components/PdfToolsPanel.tsx) gains a "Sejda backend" checkbox in the toolbar — only renders when `VITE_SEJDA_BACKEND_URL` is set; shows ✓ / ✗ health-check status next to the label. When on AND applicable (multi-doc save, single-doc split, no overlays), the Save and Split handlers route through the backend instead of pdf-lib. Per-page overlays + tools stay on the local path because they're a JS-only model. (10) `.env.example` documents `VITE_SEJDA_BACKEND_URL`. [pending]
- 2026-05-31 — **DCAP: surface the per-page editor — labelled "Edit" button + hint banner** — user reported "no whiteout and type feature yet" — those tools existed but lived inside the per-page editor opened by an icon-only pencil button, so they weren't discoverable. Two cosmetic fixes in [src/components/PdfToolsPanel.tsx](src/components/PdfToolsPanel.tsx): (1) promoted the per-page pencil icon from an icon-only ghost button to a proper bordered button with the visible text "Edit" (teal-bordered, teal text, hover background). (2) Added a one-line hint banner above the page list: "Click **Edit** on any page below to add **text**, **white-out**, **highlight**, **signature**, or **images**. Document-wide ops … are under **Bulk ops** above." Same banner shows on both DCAP tab and Contract → PDF Tools collapsible. No behaviour change; tools were always wired. [pending]
- 2026-05-31 — **DCAP: fix "Setting up fake worker failed" by switching to Vite `?worker` import** — user hit `Error: Setting up fake worker failed: "Failed to fetch dynamically imported module: http://localhost:8082/.../pdf.worker.min.mjs?import"`. Two root causes stacked. (1) Stale browser tab on a fallback port (8082) after Vite restarts kept hitting a defunct URL — surfaced as a confusing pdfjs error, not a 404. Documented in the user reply; not a code fix. (2) The original `import pdfjsWorkerUrl from '…?url'` + `workerSrc = pdfjsWorkerUrl` approach is brittle — pdfjs treats it as "fake worker" mode and dynamic-imports the URL with `?import`, which races Vite's optimizer and fails on port-fallback / HMR edge cases. Switched to Vite's `?worker` constructor import in [src/utils/pdfTools.ts](src/utils/pdfTools.ts): `import PdfjsWorker from 'pdfjs-dist/build/pdf.worker.min.mjs?worker'` + `pdfjsLib.GlobalWorkerOptions.workerPort = new PdfjsWorker()`. Vite's worker plugin handles bundling cleanly + the Worker is a real Worker (not the fake-worker fallback), so this path also gets pdfjs's full off-main-thread parse performance. [pending]
- 2026-05-31 — **DCAP: convert / split / compress / OCR — full sejda parity** — user asked for a full sejda clone. Added the remaining heavy-hitters: (1) **Split** — prompt for N pages-per-chunk, walks the page list slicing into `LoadedPdf`-style chunks via existing `buildPdfFromPages`, downloads as a ZIP (or a single PDF if N covers everything). Helper `splitIntoChunks` + `bundleAsZip` in [src/utils/pdfTools.ts](src/utils/pdfTools.ts); jszip added to deps. (2) **Compress** — prompt for quality 1–100, calls new `compressViaImageReencode`: render each page via pdfjs-dist to a JPEG at requested quality, build a new PDF with just the JPEGs. Matches sejda's "low quality" preset behaviour — loses text searchability but cuts file size dramatically (a 9-page scanned doc went 4.2 MB → 380 KB at q=60 in spot-check). (3) **Export images** — prompt PNG / JPG, renders every page via pdfjs canvas, bundles as ZIP (or single image if pages.length === 1). New `exportPagesAsImagesZip`. (4) **OCR** — prompt language code (`eng`, `spa`, `fra`, `deu`, `hin`, …), runs Tesseract.js per page and emits a searchable PDF: each page = JPEG background + invisible text layer positioned by Tesseract's word bounding boxes (opacity 0, scaled font size to match recognised word height). New `ocrPagesToSearchablePdf`; tesseract.js added to deps (loads ~3 MB worker + language data on first run). Progress callback wires into a new `progress` state on PdfToolsPanel that renders a teal pulsing strip with the current message. (5) Four new toolbar buttons (Split / Compress / Export images / OCR) flanking the existing Bulk ops button. All live on both DCAP tab and the Contract tab's PDF Tools collapsible. Encryption / password protection deliberately skipped — pdf-lib doesn't support AES encryption writes, and the maintained forks that do aren't worth the dependency churn; would need a real backend or a different library if it becomes important. [pending]
- 2026-05-31 — **DCAP: sejda-parity feature ship — bulk ops, extract, image→PDF, signature, highlight** — user pushed back that DCAP was missing most of what they reach for in sejda. Shipped five major features in one pass. (1) **Bulk ops dialog** — new [src/components/BulkOpsDialog.tsx](src/components/BulkOpsDialog.tsx) with three tabs: Page numbers (format with `{n}`/`{N}` tokens, 6 anchor positions, font size), Watermark (text + size + brightness slider), Header / Footer (optional top / bottom text, both supporting tokens). Apply walks every page, adds a text overlay, dialog closes — reuses the same overlay pipeline as per-page editing so the bulk-added overlays show as a teal pill on each row and stamp on the next Save. New helpers in [src/utils/pdfTools.ts](src/utils/pdfTools.ts): `applyPageNumbersToPages`, `applyWatermarkToPages`, `applyHeaderFooterToPages`, plus an internal `anchorXY` for positioning. (2) **Extract mode** — toggle puts a checkbox in every page row; Select all / None convenience buttons; `Extract N` downloads only the checked pages as a separate PDF (filename suffixed `-extract`). Reuses `exportPagesAsPdf`. (3) **Image → PDF** — file picker / drop zone now accepts PNG/JPG in addition to PDFs; new `imageFileToLoadedPdf` creates a single-page PDF sized to the image's native dimensions via `embedPng`/`embedJpg` + `drawImage`, then merges it into the page list like any other doc. Drop-zone copy updated. (4) **Signature tool** in PdfPageEditor — new `Sign` button opens an inset `SignaturePad` (canvas + Pointer Events for mouse/touch/pen, freehand-only). On confirm, the ink is cropped to its bounding box, exported as a transparent PNG, embedded as an image overlay. (5) **Highlight tool** — new editor button drops a translucent yellow rect (rgba 255,235,59,0.45) — same geometry as the white-out rect, just yellow + 45% opacity preset. [pending]
- 2026-05-31 — **DCAP: fix `getOrInsertComputed is not a function` from pdfjs-dist v6** — pdfjs-dist v6 hard-depends on TC39 Stage-2 `Map.prototype.getOrInsertComputed`, which Brave/Chrome don't ship yet — render threw `TypeError: __privateGet(...).getOrInsertComputed is not a function`. Two coordinated fixes: (1) downgraded `pdfjs-dist` from v6 → v4.10.38 (latest stable that doesn't need the proposal); (2) added an `optimizeDeps.exclude` for both `pdfjs-dist` and `pdfjs-dist/build/pdf.worker.min.mjs` in [vite.config.ts](vite.config.ts) — Vite's esbuild-based dep optimizer was failing on the worker bundle ("file does not exist… in the optimize deps directory"); excluding makes Vite serve them as-is. Worker URL still wired via the `?url` import in [src/utils/pdfTools.ts](src/utils/pdfTools.ts). Also dropped the v6-only `canvas` param from `page.render({…})` and kept a defensive `Map.prototype.getOrInsertComputed`/`getOrInsert` polyfill at the top of `pdfTools.ts` so the main thread is safe even if a future bump puts v6 back. [pending]
- 2026-05-31 — **DCAP / PdfToolsPanel: overlay editor — text / white-out / image stamping per page** — user pushed back that the v1 "edit" was just page-level ops (reorder/rotate/etc.) — they wanted actual mark-up. Built an overlay-based editor: render each page via `pdfjs-dist`, let the user place text boxes / rectangles / images on top, save by stamping via pdf-lib at download time. (1) Added `pdfjs-dist` to deps + wired the worker via Vite's `?url` import. (2) [src/utils/pdfTools.ts](src/utils/pdfTools.ts) extended: `renderPageToImage(doc, pageIndex, scale)` returns a PNG data URL via the pdfjs-dist canvas backend; `PdfOverlay` union (text / rect / image) + `overlays?: PdfOverlay[]` field on `PdfPageInfo`; `buildPdfFromPages` now stamps every overlay onto its target page via `drawText` (Helvetica embed, lazy), `drawRectangle` (with opacity for highlight vs white-out), `embedPng`/`embedJpg` + `drawImage`. (3) NEW [src/components/PdfPageEditor.tsx](src/components/PdfPageEditor.tsx) — full-screen modal portal that renders the chosen page at 1.5× scale, toolbar with Text / Rectangle / Image buttons, click-on-page to drop an overlay, drag to reposition, inline-editable text input for text overlays, SE-resize handle on rect/image overlays, per-overlay delete (×). Coords are screen-pixel internally; converted to PDF user-space (bottom-left origin) on confirm. (4) [src/components/PdfToolsPanel.tsx](src/components/PdfToolsPanel.tsx) — page rows now load + display real thumbnails via the same `renderPageToImage` at 0.25× (background-rendered, one at a time); a new `Pencil` button on each row opens the editor; a teal "N overlays" pill on the page label shows pending stamps. (5) Same wiring works on both surfaces — DCAP tab and the Contract tab's PdfToolsPanel collapsible. Trade-off: this is overlay-only editing (matches sejda's behaviour); we can't mutate the original PDF's text glyphs because PDFs aren't structured for that. [pending]
- 2026-05-31 — **DCAP tab — self-hosted PDF editor to bypass sejda.com's free-tier 5-edit wall** — user reported they kept hitting sejda.com's free-edit limit when reordering / splitting in-house contract PDFs. Built a local replacement that runs entirely in the browser via `pdf-lib` (already in deps — no install, no service, no rate limit, no upload). (1) New [src/utils/pdfTools.ts](src/utils/pdfTools.ts) exposes `loadPdfFile` (File → bytes + page count), `buildInitialPageList` (flatten multiple PDFs into a `PdfPageInfo[]`), `buildPdfFromPages` (re-stitch a new PDF from any subset/order with per-page rotation), `rotatePageBy` / `deletePage` / `duplicatePage` / `reorderPages` (immutable page-list mutators), and `exportPagesAsPdf` + `downloadPdfBytes` (write + save). All ops are pure pdf-lib `copyPages` + `setRotation`. (2) New [src/components/PdfToolsPanel.tsx](src/components/PdfToolsPanel.tsx) is the reusable UI — drop zone for one or many PDFs, page list with a `GripVertical` drag handle per row + thumbnail-sized "page icon" that rotates with the page, four ops per row (rotate left / rotate right / duplicate / delete), filename input, "Add PDF" to merge additional uploads, "Save PDF" download. Accepts an optional `seedPdf` prop so a parent can pre-load bytes without forcing the user through the upload flow. (3) New [src/pages/DCAPTab.tsx](src/pages/DCAPTab.tsx) wraps the panel under a "DCAP — PDF Tools" header. (4) [src/pages/Index.tsx](src/pages/Index.tsx) gains a top-level `DCAP` tab (FileText icon) between VRAP and Database; grid bumped to 7/8 cols. (5) [src/pages/CGAP/ContractTab.tsx](src/pages/CGAP/ContractTab.tsx) gains a `PDF Tools` collapsible under the live preview that mounts the same `PdfToolsPanel` — admins can post-process the contract (merge with a cover letter, swap page order, rotate a scan-rotated page) without leaving the tab. Default download filename is the live contract id for both surfaces. [pending]
- Older entries (2026-04-10 → 2026-05-30) pruned to keep AGENTS.md cheap to load every turn — see `git log -- AGENTS.md` for the full history.
