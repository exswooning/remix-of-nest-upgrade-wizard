# Copy to `.env` before `docker compose up`. Anything Next.js needs at
# build time should be `NEXT_PUBLIC_*`. Server-only secrets stay
# unprefixed and are read via `process.env` inside server components.

NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1
